/**
 * Cart store — will be implemented in Phase 2 with Zustand.
 *
 * This file is a placeholder that outlines the store's shape.
 * When we implement it, we'll use Zustand with persist middleware
 * to save the cart to localStorage across page refreshes.
 *
 * Shape:
 *   items: CartItem[]
 *   addItem(product, quantity)
 *   removeItem(productId)
 *   updateQuantity(productId, quantity)
 *   clearCart()
 *   totalItems (derived)
 *   totalPrice (derived)
 */

export {};
