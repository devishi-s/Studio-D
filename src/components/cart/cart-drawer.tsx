/**
 * CartDrawer — slide-out cart sidebar (Phase 2).
 *
 * Built with shadcn/ui Sheet component.
 * Reads from the Zustand cart store.
 * Shows CartItem list, CartSummary, and checkout CTA.
 *
 * This is a Client Component ("use client") because it
 * manages open/close state and reads from the cart store.
 */

export {};
